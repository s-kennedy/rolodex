<div class="all_contacts">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			
			<h1>Everyone you know</h1>

			<ul><% @contacts.each do |contact| %>
				<li><%= link_to(contact.name, contact_path(contact.id))%> <%= contact.email%></li>
				<% end %>
			</ul>
	
		</div>
	</div>
</div>
